import java.util.Scanner;
class PrimeClass {
   // Searching for a prime class
   private Scanner sc = new Scanner(System.in);
   public int x = getX();
   private int getX() {
      // System output
      System.out.print("Enter the starting value " + "for x: ");
      return sc.nextInt();
   }
}
